title: "A blog"
author: "A person"
description: "A blog." # This will show up in places like twitter, linkedin, etc, if you share your sites url as a post.

#######################################################
# Don't Change anything below (unless you're Nishant) #
#######################################################
theme: "minima"

rss: rss

kramdown:
  syntax_highlighter_opts:
    disable: true
    
plugins:
  - jekyll-feed
  - jekyll-sitemap

titles_from_headings:
  strip_title: true
  collections: true

defaults:
  - scope:
      path: ""
      type: post
    values:
      tags: Other
